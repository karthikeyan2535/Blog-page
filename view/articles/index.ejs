<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog Website</title>
</head>

<body>
    <style>
        *{
        margin: 0;
        padding: 0;
        }
        body{
        background: linear-gradient(to right, #b6e8b6, #71d72c);
        display: flex;
        align-items: center;
        flex-direction: column;
        height: 100vh;
        font-family: 'Rubik', sans-serif;
        }
        .container{
        margin-top: 80px;
        }
        .nav1{
        width: 100vw;
        padding: 7px;
        z-index: 2;
        height: 50px;
        display: flex;
        justify-content: flex-end;
        flex-direction: row;
        flex-wrap: wrap;
        flex-flow: row;
        overflow: hidden;
        position: fixed;
        gap: 25px;
        font-size: 20px;
        background-color: rgb(20, 18, 18);
        color: white;
        font-family: 'Lato', sans-serif;
    }
    
    .nav1 div{
        margin: 7px;
        cursor: pointer;
    }
    a{
        text-decoration: none;
        color: white;
    }
    .btn{
        background: linear-gradient(to right, #677ce8, #0e0ae8);
        color: #fff;
        font-size: 12px;
        padding: 10px 14px;
        margin-left: 20px;
        border: 1px solid transparent;
        border-radius: 8px;
        font-weight: 600;
        box-shadow: #03279d;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        margin-top: 10px;
        cursor: pointer;
        font-family:'Rubik', sans-serif ;
    }
    .btn-success{
        padding: 12px;
        border-radius: 11px;
    }
    .btn-danger{
        background: linear-gradient(to right, #7e343e, #ec0505);
        padding: 7px;
        border-radius: 15px;
    }
    .btn-info{
        background: linear-gradient(to right, #65ec7c, #53f502);
        padding: 10px;
        border-radius: 15px;
    }
    .card{
        border-radius: 15px;
        background: linear-gradient(to right, #1c1c1c, #080505);
    }
    .card-body{
        background: linear-gradient(to right, #1c1c1c, #080505);
        color: #fff;
        border-radius: 15px;
    }
    .text-muted{
        color: #0e0ae8;
    }
        </style>

    <div class="nav1">
    <div class="nb"><a href="index.ejs">Home</a></div>
    <div class="nb"><a href="index.ejs">My Blogs</a></div>
    <div class="nb">Explore</div>
    <div class="nb">Help</div>
    </div>

    <div class="container">
        <h1 class="mb-4">Welcome to my blog page!!</h1>
        <a href="/articles/new" class="btn btn-success">Create Article</a>

        <% articles.forEach(article=>{%>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title">
                        <%=article.title%>
                    </h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%=article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2">
                        <%=article.description%>
                    </div>
                    <a href="articles/<%=article.slug%>" class="btn btn-primary">Read More</a>
                    <a href="articles/edit/<%= article.id%>" class="btn btn-info">Edit</a>
                    <form action="/articles/<%=article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>

            <%})%>
    </div>
</body>

</html>